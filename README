* build containers with:

    docker-compose build

* start containers with:

    docker-compose up -d

* run a puppet agent:

    docker run --net puppetmasterdocker_default puppet/puppet-agent-debian

* stop them with:

    docker-compose down

